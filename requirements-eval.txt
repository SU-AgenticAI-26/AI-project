# Evaluation suite — install in addition to requirements.txt:
#   pip install -r requirements-eval.txt
# or use the project's uv eval extra:
#   uv sync --extra eval
#
# Eval-only dependencies such as tiktoken live here rather than in
# requirements.txt. Not needed to run the Streamlit app.

-r requirements.txt

# ── LLM-as-judge evaluation frameworks ───────────────────────────────────────
ragas
deepeval

# ── Token counting (eval_provider.py cost estimates) ─────────────────────────
tiktoken>=0.5.0
