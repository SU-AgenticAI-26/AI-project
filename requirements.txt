# ── Core application ──────────────────────────────────────────────────────────
streamlit>=1.32.0
langgraph~=1.1
requests>=2.31.0

# ── LangChain stack ───────────────────────────────────────────────────────────
langchain-core~=1.2
langchain-openai~=1.1             # ChatOpenAI + OpenAIEmbeddings (default provider)
langchain-community~=0.4          # FAISS vectorstore + HuggingFaceEmbeddings (local/Claude provider)
langchain-text-splitters~=1.1     # RecursiveCharacterTextSplitter

# ── Vector store ──────────────────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── Visualisation ─────────────────────────────────────────────────────────────
pyvis>=0.3.2                     # Interactive knowledge-graph network view

# ── Optional extras (install separately only if needed) ───────────────────────
# Conference paper search:
# pip install openreview-py acl-anthology
#
# Local embeddings for Claude/local provider:
# pip install sentence-transformers
#
# Additional LLM providers:
# pip install "langchain-anthropic~=1.4"
# pip install "langchain-google-genai~=4.2"
