# ── Core application ──────────────────────────────────────────────────────────
streamlit>=1.32.0
langgraph~=1.1
requests>=2.31.0

# ── LangChain stack ───────────────────────────────────────────────────────────
langchain-core~=1.2
langchain-openai~=1.1             # ChatOpenAI + OpenAIEmbeddings (default provider)
langchain-community~=0.4          # FAISS vectorstore + HuggingFaceEmbeddings (local/Claude provider)
langchain-text-splitters~=1.1     # RecursiveCharacterTextSplitter

# ── Vector store ──────────────────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── Embeddings for local / Claude provider ────────────────────────────────────
sentence-transformers>=2.2.0     # HuggingFace all-MiniLM-L6-v2 used by HuggingFaceEmbeddings

# ── Visualisation ─────────────────────────────────────────────────────────────
pyvis>=0.3.2                     # Interactive knowledge-graph network view

# ── Optional LLM providers (install the ones you need) ────────────────────────
langchain-anthropic~=1.4
langchain-google-genai~=4.2
